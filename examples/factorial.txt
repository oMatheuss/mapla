extern func printf(fmt: char*, arg: int);

func fact(acc: int, n: int): int do
    if n < 2 then return acc; end
    return fact(n * acc, n - 1);
end

func main(): int do
    int r = fact(1, 5);
    printf("factorial of 5 is %d\n", r);
    return 0;
end