extern func printf(fmt: char*, ...);

func main(): int do
    for x = 2 to 100 do
        var y = 2;
        while y * y < x && x % y > 0 do
            y += 1;
        end
        if y * y > x then
            printf("%d\n", x);
        end
    end

    return 0;
end
