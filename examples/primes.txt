use io;

func main() do
    for x = 2 to 100 then
        int y = 2;
        while y * y < x && x % y > 0 then
            y += 1;
        end
        if y * y > x then
            printInt(x);
            printChar(10);
        end
    end
end