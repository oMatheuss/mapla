extern func printf(fmt: char*, arg: int);

func fib(num: int): int do
    if num < 2 then return num; end
    return fib(num - 1) + fib(num - 2);
end

func main(): int do
    int r = fib(10);
    printf("fibonacci of 10 is %d\n", r);
    return 0;
end